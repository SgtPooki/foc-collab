# Committed runtime config. Sourced by the publish skill and spike scripts
# before every command. Do not edit values here per-invocation; secrets go
# in .env (gitignored), see .env.example.
NETWORK=calibration
