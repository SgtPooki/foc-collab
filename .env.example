# Copy to .env (gitignored). Never commit .env.
PRIVATE_KEY=0xYOUR_PRIVATE_KEY
# apps/corgi: a second throwaway wallet with a little tFIL and USDFC that the
# acceptance test and scripts/feed.mjs use to feed the corgi.
FEEDER_KEY=0xYOUR_FEEDER_PRIVATE_KEY
